export const metadata = {
  title: "My Bookings - Care.xyz",
  description: "View your service booking history and status.",
};

export default function MyBookingsLayout({ children }) {
  return <>{children}</>;
}
